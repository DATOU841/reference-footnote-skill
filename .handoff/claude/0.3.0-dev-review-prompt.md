# Claude 审查提示词：ReferenceFootnote 0.3.0-dev 二轮 RAG 闭环

请审查仓库：

```text
/Users/a13497/Desktop/skill工作区/reference-footnote-skill
```

版本：

```text
0.3.0-dev
```

## 审查目标

本轮重点审查 0.3.0-dev 是否补齐协同闭环：

1. 是否新增补库后二轮 RAG 返回后的端到端离线 fixture。
2. fixture 是否真实演练 `build-post-ingestion-rag-call -> validate-rag-response -> build-evidence-map -> plan-footnotes`。
3. 是否仍然不执行真实 RAG 查询或真实检索入库。
4. coordinator / rag-interpreter agent 是否足以指导 A7.5 / A8.5 协同阶段。
5. `allowed_server_entry_if_authorized` 是否只作为下游 `检索入库` 元数据，而非 ReferenceFootnote 执行路径。
6. 是否保持 0.2.0-dev 的边界、质量门禁和防伪造机制。

## 硬性限制

只做只读审查，不要修改文件，不要提交，不要发布，不要安装 runtime，不要操作服务器。

不要运行 CNKI、WoS、Zotero、PDF 获取、RAG 导库、writing-pool、advance-pool、mimo。

不要读取或操作 `openclaw-cnki-takeover`。

不要探测 `localhost:22`。

## 建议读取

- `tests/run-fixtures.py`
- `tests/fixtures/mocks/post-ingestion-rag-response.json`
- `agents/coordinator.md`
- `agents/rag-interpreter.md`
- `docs/collaboration-flow.md`
- `docs/handoff-protocol.md`
- `references/rag-platform-interface.md`
- `scripts/build-post-ingestion-rag-call.py`
- `scripts/validate-rag-response.py`
- `scripts/build-evidence-map.py`
- `scripts/plan-footnotes.py`

## 建议离线命令

```bash
cd /Users/a13497/Desktop/skill工作区/reference-footnote-skill
python3 -m py_compile scripts/*.py tests/run-fixtures.py
python3 scripts/verify-structure.py --target reference-footnote
python3 scripts/startup.py
python3 tests/run-fixtures.py --all
python3 scripts/run-local-gate.py --pre-review
```

预期：全部通过，19 个 fixture passed，无外部调用。

## 输出

请把审查结果写入：

```text
/Users/a13497/Desktop/skill工作区/reference-footnote-skill/.handoff/claude/0.3.0-dev-review.md
```

格式：

```markdown
# ReferenceFootnote 0.3.0-dev 二轮 RAG 闭环审查

结论：APPROVED | APPROVED_WITH_NOTES | CHANGES_REQUESTED

## P0

## P1

## P2

## Gate 结果

## 发布建议
```
