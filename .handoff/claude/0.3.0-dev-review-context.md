# ReferenceFootnote 0.3.0-dev 审查上下文

仓库：

```text
/Users/a13497/Desktop/skill工作区/reference-footnote-skill
```

当前分支：

```text
codex/0.3.0-dev-rag-closure
```

版本：

```text
0.3.0-dev
```

## 本轮目标

继续修复协同能力，重点补齐 0.2.0-dev 审查中的 P2：

- 补库后二轮 RAG 返回后，离线演练 `validate-rag-response -> build-evidence-map -> plan-footnotes` 的证据重建闭环。
- 扩写 `coordinator` 和 `rag-interpreter` agent 指引，覆盖 A7.5/A8.5。
- 文档化 `allowed_server_entry_if_authorized` 和调用包 schema。

## 已实现

- 新增 `tests/fixtures/mocks/post-ingestion-rag-response.json`。
- 新增 fixture_19：`post-ingestion-evidence-closure`。
- 扩写 `agents/coordinator.md`。
- 扩写 `agents/rag-interpreter.md`。
- 更新 `docs/collaboration-flow.md`、`docs/handoff-protocol.md`、`references/rag-platform-interface.md`。
- 版本推进到 `0.3.0-dev`。

## 边界

本轮仍只做离线 skill 本体开发。不运行真实 CNKI、WoS、Zotero、PDF 获取、RAG 查询、RAG 导库、writing-pool、advance-pool、mimo 或服务器部署。

## 预期本地验证

```bash
python3 -m py_compile scripts/*.py tests/run-fixtures.py
python3 scripts/verify-structure.py --target reference-footnote
python3 scripts/startup.py
python3 tests/run-fixtures.py --all
python3 scripts/run-local-gate.py --pre-review
```

预期：19 个 fixture 全部通过。
