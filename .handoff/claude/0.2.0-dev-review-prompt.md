# Claude 审查提示词：ReferenceFootnote 0.2.0-dev 协同调用层

请审查仓库：

```text
/Users/a13497/Desktop/skill工作区/reference-footnote-skill
```

版本：

```text
0.2.0-dev
```

## 审查目标

本轮重点审查 ReferenceFootnote 的协同能力是否足够清晰、可执行、且不越界：

1. 是否能把 evidence gap 转换成可交给 `检索入库` 的正式调用包。
2. 是否能生成给 `检索入库` 的中文调用提示词。
3. 是否明确真实 CNKI/WoS/Zotero/PDF/RAG 导库只能由 `检索入库` 在用户授权后执行。
4. 是否能在 `检索入库` 返回 completion 后，只针对 `rag_indexed=true` 的结果准备补库后二轮 RAG 反查调用包。
5. 是否没有伪装成已经完成真实检索或真实 RAG 查询。
6. 是否保留 0.1.0-dev 的所有边界和质量门禁。

## 硬性限制

只做只读审查，不要修改文件，不要提交，不要发布，不要安装 runtime，不要操作服务器。

不要运行 CNKI、WoS、Zotero、PDF 获取、RAG 导库、writing-pool、advance-pool、mimo。

不要读取或操作 `openclaw-cnki-takeover`。

不要探测 `localhost:22`。

## 建议读取

- `SKILL.md`
- `docs/collaboration-flow.md`
- `docs/handoff-protocol.md`
- `references/search-intake-interface.md`
- `references/rag-platform-interface.md`
- `scripts/build-search-intake-call.py`
- `scripts/build-post-ingestion-rag-call.py`
- `scripts/apply-intake-completion.py`
- `tests/run-fixtures.py`

## 建议离线命令

```bash
cd /Users/a13497/Desktop/skill工作区/reference-footnote-skill
python3 -m py_compile scripts/*.py tests/run-fixtures.py
python3 scripts/verify-structure.py --target reference-footnote
python3 scripts/startup.py
python3 tests/run-fixtures.py --all
python3 scripts/run-local-gate.py --pre-review
```

预期：全部通过，18 个 fixture passed，无外部调用。

## 输出

请把审查结果写入：

```text
/Users/a13497/Desktop/skill工作区/reference-footnote-skill/.handoff/claude/0.2.0-dev-review.md
```

格式：

```markdown
# ReferenceFootnote 0.2.0-dev 协同调用层审查

结论：APPROVED | APPROVED_WITH_NOTES | CHANGES_REQUESTED

## P0

## P1

## P2

## Gate 结果

## 发布建议
```
