你有写入权限，请将结果写入：/Users/a13497/Desktop/skill工作区/reference-footnote-skill/.handoff/claude/0.5.3-dev-review.md

请对 ReferenceFootnote 0.5.3-dev 做发布前审查。

仓库路径：
/Users/a13497/Desktop/skill工作区/reference-footnote-skill

审查上下文：
/Users/a13497/Desktop/skill工作区/reference-footnote-skill/.handoff/claude/0.5.3-dev-review-context.md

本版本修复目标：
- 2.5 RAG 导库完成后，ReferenceFootnote 默认执行只读 RAG reverse lookup。
- 不再把 RAG 反查结果称为需要用户提供的“回执”。
- executor 内部写入 `state/rag-calls/<batch>.response.json`，再由 `validate-rag-response.py` 消费。
- 缺 live RAG 配置时，阻断为 `missing_rag_executor_config`，不得要求用户手工提供 response。
- RAG ingestion 仍然禁止，仍属于 `检索入库`。

请重点审查：
1. `SKILL.md`、`docs/state-machine.md`、`docs/rag-protocol.md`、`docs/boundary-rules.md`、`docs/collaboration-flow.md` 是否一致。
2. `config/boundaries.yaml` 是否准确区分 `rag_ingestion` 和 `rag_reverse_lookup_query`。
3. `scripts/run-rag-reverse-lookup.py` 是否符合边界：不导库、不取 PDF、不操作 CNKI/WoS/Zotero，只做 post-2.5 RAG query。
4. `build-post-ingestion-rag-call.py` 是否已经去掉 `prepared_not_executed` / `requires_external_rag_operator` 的错误语义。
5. `tests/run-fixtures.py` 新增 fixture 是否覆盖 mock executor、缺配置 blocker、response validation 和边界。
6. 是否允许 Codex 安装 local runtime，并继续当前 golden-grid 文章任务。

硬边界：
- 不运行真实 CNKI/WoS/Zotero/PDF 获取。
- 不运行 RAG 导库。
- 不操作 openclaw-cnki-takeover。
- 不探测 localhost:22。
- 不执行 staging/production。
- 本次只做审查，不修改代码。

输出要求：
- 第一行给出结论：`APPROVED` / `APPROVED_WITH_NOTES` / `CHANGES_REQUESTED`。
- 若有问题，按 P0/P1/P2 分级并给出文件路径。
- 明确回答：是否允许安装 local runtime；是否允许继续 golden-grid 任务。
- 完成后只回复：已写入、文件路径、结论、Codex 下一步。
