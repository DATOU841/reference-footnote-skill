# Claude Review Prompt for ReferenceFootnote 0.5.2-dev

请审查 `/Users/a13497/Desktop/skill工作区/reference-footnote-skill` 的 0.5.2-dev 修复。

你有写入权限，请将审查结果写入：

`/Users/a13497/Desktop/skill工作区/reference-footnote-skill/.handoff/claude/0.5.2-dev-review.md`

本次目标是修复流程顺畅性、独立写作池能力、mimo-style evidence trace ledger、风险清理闭环和最终交付收束。

请重点审查：

1. `SKILL.md` 的 S00-S120 工作流是否清晰、可执行、无断裂。
2. `docs/state-machine.md` 与脚本是否一致。
3. `evidence-trace-ledger.json` 的设计是否能作为全文顺序唯一主线。
4. 独立写作池式审查是否没有依赖 `正文写作` skill、`03-writer`、guard、ledger、advance-pool 或 mimo。
5. 风险清单是否形成了 inventory -> cleanup plan -> cleanup result -> cleaned artifacts -> final gate 的闭环。
6. `analogy_only`、`ownership_unverified`、未消费参考文献、写作池要求完整段落重写是否被最终 gate 阻断。
7. 最终交付是否只保留主入口，过程文件是否进入 `delivery/process/`。
8. 离线 fixture 是否覆盖核心边界，是否还缺关键 fixture。

请输出结论：

- `APPROVED`
- `APPROVED_WITH_NOTES`
- `CHANGES_REQUESTED`

如需修改，请列出阻断项和建议修复顺序。

写入完成后，请在回复中只告诉我：

- 是否已写入
- 文件路径
- 审查结论
- Codex 下一步执行重点
