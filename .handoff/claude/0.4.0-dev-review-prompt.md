请对 `/Users/a13497/Desktop/skill工作区/reference-footnote-skill` 的 `0.4.0-dev` 进行发布前审查。

审查重点：

1. 是否准确落实“脚注是正文内容必要补充，不是参考文献脚注/题录罗列”。
2. `footnote`、`endnote`、`reference_only` 的边界是否清楚，`reference_only` 是否被禁止进入脚注正文。
3. 候选脚注 15-25 个、裁剪后约 15 个、参考文献 25-30 篇的重要性裁剪逻辑是否合理。
4. “入库材料可消费正文级文本池均值不低于 200 字/篇”的实现是否合理；当前按池级 warning 处理，单篇低材料只传递风险，不硬拒绝。
5. PDF + RAG 双核验是否保持正确边界：ReferenceFootnote 只生成请求、接收结构化回执和输出问题清单，不直接获取 PDF 或运行真实 RAG。
6. 与 `检索入库`、`正文写作` 的协同协议是否充分，是否遗漏必要字段。
7. 新增脚本和 fixture 是否覆盖：低材料、脚注裁剪、空泛背景删除、`reference_only` 禁入、参考文献裁剪、真实性通过、PDF/RAG 冲突、页码/OCR 风险、插入位置风险、未消费参考文献、最终交付包。
8. 是否存在 P0/P1 发布阻断问题、边界越权、字段不一致、阶段机顺序错误、测试虚假通过或中文乱码。

请输出：

- 审查结论：`APPROVED`、`APPROVED_WITH_NOTES` 或 `CHANGES_REQUESTED`
- P0/P1/P2 问题列表，带文件路径和修复建议
- 对是否允许 Codex 进入 0.4.0-dev 第一版 git 发布的明确意见

参考上下文文件：

`/Users/a13497/Desktop/skill工作区/reference-footnote-skill/.handoff/claude/0.4.0-dev-review-context.md`
