请对 `/Users/a13497/Desktop/skill工作区/reference-footnote-skill` 的 `0.5.0-dev` 进行发布前审查。

审查背景：

用户指出 0.4.0-dev 的流程倒装。正确流程应当是：

1. 读取已写文章。
2. 从文章反推检索方向、关键词、来源类型和证据需求。
3. 先交给 `检索入库` 建设足量文献库。
4. 等待入库完成并通过质量验收。
5. 再做 RAG 反查、证据映射、脚注候选、参考文献裁剪和风险清单。

请重点审查：

- 0.5.0-dev 是否真正实现 retrieval-first，而不是只写文档。
- `build-rag-request.py` 是否在无 `intake-status.json` 且无用户声明已有 RAG 库时阻断。
- `build-search-blueprint.py`、`build-initial-search-handoff.py`、`validate-intake-quality.py` 是否满足规划。
- `search_intake_library_build` 与 `search_intake_gap` 协议是否区分清楚。
- `validate-citation-plan.py` 是否把缺 blueprint、initial-library handoff、intake completion 作为 blocking。
- 41 个 fixture 是否覆盖关键流程和边界。
- 是否仍遵守不运行真实 CNKI/WoS/Zotero/PDF/RAG、不操作 openclaw、不探测 localhost:22、不部署服务器、staging/production blocked。

请输出：

- 审查结论：`APPROVED`、`APPROVED_WITH_NOTES` 或 `CHANGES_REQUESTED`
- P0/P1/P2 问题列表，带文件路径和修复建议
- 是否允许 Codex 进入 0.5.0-dev git 发布

参考上下文：

`/Users/a13497/Desktop/skill工作区/reference-footnote-skill/.handoff/claude/0.5.0-dev-review-context.md`
